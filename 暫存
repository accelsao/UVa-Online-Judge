#include<iostream>
#include<string>
struct word{
	string s,int y;
	bool operator<(const word &a)const{
	if(y==a.y)return
	}
}b[101],p[101];
using namespace std;
int main(){
	int t;
	while(t--){
	int n;
	cin>>n;
	for(int i=0;i<n;i++){
		cin>>b[i].s>>p[i].y;
	    p[i].s=g(b[i].s);
	}
	sort(p,p+n);
	
	
	}
	
}
