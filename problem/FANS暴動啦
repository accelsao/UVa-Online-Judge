#include<iostream>
#include<cmath>
#include<algorithm>
#include<cstring>
#include<cstdio>
#include<cstdlib>
#include<queue>
#include<string>
#include<vector>
using namespace std;

struct state{
	int c[4], d;
	bool operator<(const state &a)const{
		return d > a.d;
	}
};
int cap[4];
int vis[101][101][101], ans[101];//總水量不變 vis紀錄前兩個就好
void solve(int a, int b, int c, int d,int e){
	cap[0] = a; cap[1] = b; cap[2] = c; cap[3] = d;
	memset(vis, 0, sizeof(vis));
	memset(ans, -1, sizeof(ans));
	priority_queue<state>q;

	state s;
	s.d = s.c[0] = s.c[1] = s.c[2] = 0;
	s.c[3] = d;
	q.push(s);
	vis[0][0][0] = 1;
	while (!q.empty()){
		state x = q.top(); q.pop();
		for (int i = 0; i < 4; i++){
			int d = x.c[i];
			if (ans[d] < 0 || x.d < ans[d])
				ans[d] = x.d;
		}
		if (ans[e] >= 0)break;//find answer
		for (int i = 0; i < 4; i++)
			for (int j = 0; j < 4;j++)
				if (i != j){
					if (!x.c[i] || x.c[j] == cap[j])continue;//i倒入j i==0 or j滿 跳過
					int num = min(cap[j], x.c[i] + x.c[j]) - x.c[j];//能倒過去的量
					state next;
					memcpy(&next, &x, sizeof(x));
					next.c[i] -= num;
					next.c[j] += num;
					next.d = x.d + num;
					if (!vis[next.c[0]][next.c[1]][next.c[2]]){
						vis[next.c[0]][next.c[1]][next.c[2]] = 1;
						q.push(next);
					}
				}

	
	}
	while (e >= 0){
		if (ans[e]>=0){
			printf("%d %d\n", ans[e], e);
			return;
		}
		e--;
	}
} 

int main(){
	FILE *stream;
		//freopen_s(&stream, "c24502in.txt", "r", stdin);
		//freopen_s(&stream, "c24502out.txt", "w", stdout);

	int t;
		srand(0);
		//t = 3000;



	cin >> t;
		//printf("%d\n", t);
	int a, b, c, d, e;
	while (t--){
	/*	srand(t);
		a = rand() % 100 + 1;
		b = rand() % 100 + 1;
		c = rand() % 100 + 1;
		d = rand() % 100 + 1;
		e = rand() % 100 + 1;
		printf("%d %d %d %d %d\n", a, b, c, d, e);*/

		
		
		cin >> a >> b >> c >> d >> e;
		solve(a, b, c, d, e);
	}
	//fclose(stdin);
	//fclose(stdout);
}
/*
2
2 3 4 5 2
62 3 199 63 61

2 2
182 61
*/
/*
62 3 199 63 

62 0 0 1  62
59 3 0 1  65
0 3 59 1 124
1 3 59 0 125
1 2 59 1 126
1 0 61 1 128

AKB48是日本大型女子偶像團體 團名取自東京的秋葉原 Akihabara
AKB48於2013年3月，獲金氏世界紀錄認證為世界上最多成員的流行團體
在日本締造出巨大人氣 甚至紅到海外
這可怕的粉絲數量 常常讓表演現場造成混亂
於是 現場進行人數管制 全部分成4大搖滾區  每塊都有人數上限 
一開始觀眾會先進入第四區 人數剛好是上限
現在 為了讓後面的人進來 要分點人到其他三區
為了不讓現場發生混亂且方便管制 人會一整區一起移動
除非目的區塊已經滿了 剩下的才會留在原地 根據現場和預測將到來人數
要求某一區限制進入特定人數
能不能進行一些移動 讓某一區的人數達到要求 
可能會有達不到要求的情況
那就只好盡可能的滿足要求
為了讓公演順利結束 
加油吧...

輸入第一行T代表有T組測資
每組測資有5個數字
前4個數字(小於500)代表 i-th 的區塊 
第5個數字(小於<500)代表要求的人數

輸出一行有兩個數字
第一個代表過程中移動的人數和
第二個數字代表 等於或最接近要求 的人數
*/
/*


*/
